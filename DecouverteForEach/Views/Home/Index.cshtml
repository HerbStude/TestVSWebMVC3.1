@{
    ViewData["Title"] = "Home Page";
}

<div class="row">
    @foreach (var item in ViewBag.MonTableau)
    {
        <a href="#" class="card border m-1 text-center" style="width: 7rem;">
            <div class="card-body">
                @item
                Mon contenu
            </div>   
        </a>
    }
</div>
